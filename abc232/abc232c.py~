import itertools

N, M = map(int, input().split())
tkBalls = []
aoBalls = []

for i in range(M):
    tkBalls.append(list(map(int, input())))

for i in range(M):
    aoBalls.append(list(map(int, input())))

# 1 - 2, 3, 4, ... N
answer = []
# initialize
answer = list(itertools.repat(list(itertools.repat(0, N)), M))
print(answer)
